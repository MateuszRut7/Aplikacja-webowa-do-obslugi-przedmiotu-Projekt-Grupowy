# backend

Aby otworzyć projekt należy uruchomić komendy

pip install -r required.txt

python manage.py migrate
python manage.py runserver

Do utworzenia użytkowników użyć komendy
python manage.py shell < makeusers.py